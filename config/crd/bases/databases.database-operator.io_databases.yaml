---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.1
  name: databases.databases.database-operator.io
spec:
  group: databases.database-operator.io
  names:
    kind: Database
    listKind: DatabaseList
    plural: databases
    shortNames:
    - db
    singular: database
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .spec.version
      name: Version
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.readyReplicas
      name: Ready
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Database is the Schema for the databases API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DatabaseSpec defines the desired state of Database.
            properties:
              elasticsearch:
                description: Elasticsearch specific configuration
                properties:
                  clusterName:
                    description: ClusterName specifies the Elasticsearch cluster name
                    type: string
                  nodeRoles:
                    description: NodeRoles specifies the roles for this node (master,
                      data, ingest)
                    items:
                      type: string
                    type: array
                  parameters:
                    additionalProperties:
                      type: string
                    description: Additional Elasticsearch configuration parameters
                    type: object
                type: object
              env:
                description: Environment variables to set in the database container
                items:
                  description: EnvVar defines an environment variable
                  properties:
                    name:
                      description: Name of the environment variable
                      type: string
                    value:
                      description: Value of the environment variable
                      type: string
                    valueFrom:
                      description: ValueFrom specifies a source for the environment
                        variable's value
                      properties:
                        secretKeyRef:
                          description: SecretKeyRef selects a key from a secret
                          properties:
                            key:
                              description: Key in the secret to use
                              type: string
                            name:
                              description: Name of the secret
                              type: string
                          required:
                          - key
                          - name
                          type: object
                      type: object
                  required:
                  - name
                  type: object
                type: array
              mongodb:
                description: MongoDB specific configuration
                properties:
                  database:
                    description: Database name to create
                    type: string
                  parameters:
                    additionalProperties:
                      type: string
                    description: Additional MongoDB configuration parameters
                    type: object
                  passwordSecret:
                    description: Password secret reference
                    properties:
                      key:
                        description: Key in the secret to use
                        type: string
                      name:
                        description: Name of the secret
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  replicaSetName:
                    description: ReplicaSet name for MongoDB replica set
                    type: string
                  username:
                    description: Username for the database
                    type: string
                type: object
              postgresql:
                description: PostgreSQL specific configuration
                properties:
                  database:
                    description: Database name to create
                    type: string
                  parameters:
                    additionalProperties:
                      type: string
                    description: Additional PostgreSQL configuration parameters
                    type: object
                  passwordSecret:
                    description: Password secret reference
                    properties:
                      key:
                        description: Key in the secret to use
                        type: string
                      name:
                        description: Name of the secret
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  username:
                    description: Username for the database
                    type: string
                type: object
              redis:
                description: Redis specific configuration
                properties:
                  mode:
                    default: standalone
                    description: Mode specifies Redis mode (standalone, sentinel,
                      cluster)
                    enum:
                    - standalone
                    - sentinel
                    - cluster
                    type: string
                  parameters:
                    additionalProperties:
                      type: string
                    description: Additional Redis configuration parameters
                    type: object
                  passwordSecret:
                    description: Password secret reference
                    properties:
                      key:
                        description: Key in the secret to use
                        type: string
                      name:
                        description: Name of the secret
                        type: string
                    required:
                    - key
                    - name
                    type: object
                type: object
              replicas:
                default: 1
                description: Replicas specifies the number of database replicas
                format: int32
                maximum: 10
                minimum: 0
                type: integer
              resources:
                description: Resources defines the compute resources for the database
                properties:
                  cpu:
                    description: CPU resource request
                    type: string
                  cpuLimit:
                    description: CPU resource limit
                    type: string
                  memory:
                    description: Memory resource request
                    type: string
                  memoryLimit:
                    description: Memory resource limit
                    type: string
                type: object
              sqlite:
                description: SQLite specific configuration
                properties:
                  databaseFile:
                    description: DatabaseFile specifies the SQLite database file path
                    type: string
                  parameters:
                    additionalProperties:
                      type: string
                    description: Additional SQLite configuration parameters
                    type: object
                type: object
              storage:
                description: Storage defines the storage configuration for the database
                properties:
                  accessMode:
                    default: ReadWriteOnce
                    description: AccessMode specifies the access mode for the volume
                    type: string
                  size:
                    description: Size specifies the size of the persistent volume
                    type: string
                  storageClassName:
                    description: StorageClass specifies the storage class to use
                    type: string
                required:
                - size
                type: object
              type:
                description: Type specifies the database type (PostgreSQL, MongoDB,
                  Redis, Elasticsearch, SQLite)
                enum:
                - PostgreSQL
                - MongoDB
                - Redis
                - Elasticsearch
                - SQLite
                type: string
              version:
                description: Version specifies the version of the database to deploy
                minLength: 1
                type: string
            required:
            - type
            - version
            type: object
          status:
            description: DatabaseStatus defines the observed state of Database.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the database's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              connectionString:
                description: ConnectionString provides connection information (without
                  credentials)
                type: string
              message:
                description: Message provides additional information about the current
                  state
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  for this database
                format: int64
                type: integer
              phase:
                description: Phase represents the current phase of the database
                type: string
              readyReplicas:
                description: ReadyReplicas is the number of ready database replicas
                format: int32
                type: integer
              serviceName:
                description: ServiceName is the name of the service created for the
                  database
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
